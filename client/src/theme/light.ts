import { createTheme } from '@mui/material';

export const light = createTheme({
  bg: '#f9f9f9',
  bgLighter: 'white',
  text: 'black',
  textSoft: '#606060',
  soft: '#f5f5f5',
});

export default light;
